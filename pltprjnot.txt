point a = (int,int2,"x","red")

line l = (a,(3,4),"x","red")
tuple a = (1,2)
a.add(s,d,g)
l.fstp((1,2kl))
l.first=1,2


stment
1.vdecl
2.exp;
3.stmt

check :
variable type not void
variable name not duplicate

stmt id assign id must declare before use


fundcel

function built_in name not use
function define built in
function local variable check stment
	return type same as declare

let strmap = List.iter (fun x -> ignore(let a= StringMap.add x 123 b in b=a)) a;;

let print_users key times =
		print_int times;
	    print_endline(" " ^ key)
	in StringMap.iter print_users mymap